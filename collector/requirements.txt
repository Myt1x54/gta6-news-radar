# GTA 6 News Radar - collector dependencies (Python 3.12)
feedparser==6.0.11        # RSS / Atom parsing
httpx==0.27.2             # HTTP client (feeds, reddit json, apis)
rapidfuzz==3.10.1         # fuzzy title matching for dedupe/clustering
trafilatura==1.12.2       # article text extraction (only when no feed)
supabase==2.9.1           # Supabase Postgres + Auth client
pydantic==2.9.2           # validate AI JSON output
PyYAML==6.0.2             # read sources.yaml
python-dotenv==1.0.1      # load .env locally
tzdata==2024.2           # timezone database for zoneinfo (quiet hours on Windows)
google-genai==0.8.0       # Gemini (primary AI)
groq==0.11.0              # Groq (fallback AI, OpenAI-compatible)

# ---- dev / test ----
pytest==8.3.3
